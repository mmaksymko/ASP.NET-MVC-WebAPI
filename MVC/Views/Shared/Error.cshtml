@model LiBaby.ViewModels.ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Помилка.</h1>
<h2 class="text-danger">При обробці запиту відбулась помилка.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>ID запиту:</strong> <code>@Model.RequestId</code>
    </p>
}


<h3>Режим розроблення</h3>
<p>
	При переході до режиму <strong>розробки</strong> середовище відображатиме детальнішу інформацію про помилки.
</p>
<p>
	<strong>При розгортанні програми середовище розроблення має бути вимкнене.</strong>
	Воно може відображати чутливу інформацію винятків користувачам.
	Для локального лагодження увімкність середовище <strong>розроблення</strong> встановивши змінну <strong>ASPNETCORE_ENVIRONMENT</strong> середовища на <strong>Development</strong>
	і перезавантаживши додаток.
</p>


